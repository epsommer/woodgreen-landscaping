{
  "compilerOptions": {
    "typeRoots": [
      "node_modules/@types",
      "types"  // Make sure TypeScript includes the types directory
    ],
    "strict": true
  },
  "include": [
    "src/**/*",
    "types/**/*.d.ts" // Ensure TypeScript includes the declaration files
  ]
}
